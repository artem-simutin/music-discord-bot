class ExceptionHandler {
  /**
   * ===========================================
   * There should be exception handler that
   * writes all logs to the Mongo DB.
   *
   * _Also should be command that provides_
   * _log info to the discord chat_
   *
   * Problem is with organizing a arch that
   * includes message instance to reply
   * instantly when something went wrong
   * without providing message into this class
   * ===========================================
   */
}

export default ExceptionHandler
